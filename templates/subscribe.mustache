<script src="/lib/choices.js"></script>
<link href="/stylesheets/choices.css" rel="stylesheet">
<link href="/stylesheets/subscribe.css" rel="stylesheet">
<link href="/stylesheets/status-p.css" rel="stylesheet">

<style>
  form {
    width: 42em;
    margin: auto;
  }

  select,
  input {
    background-color: transparent;
    padding: 7px 10px;
    font-size: medium;
    border: 1px solid lightgray;
    border-radius: 2px;
    display: block;
    margin: 10px;
    width: 300px;
    box-sizing: border-box;
  }

  select:invalid,
  input:invalid {
    border: solid 1px #f59d9d !important;
  }

  button[type="submit"] {
    text-align: center;
    margin: 1em;
    background-color: var(--the-green);
    color: white !important;
    text-decoration: none;
    padding: 0.9em 1.3em;
  }

  button[type="submit"]:disabled {
    opacity: 0.3;
  }

  section {
    background-color: #ffffff !important;
  }

  dialog main {
    background-color: white;
    margin: auto;
    max-width: 1200px;
    width: fit-content;
    width: -moz-fit-content;
    margin-top: 80px !important;
  }

  dialog header {
    padding: 1em;
    padding-left: 1.3em;
    font-size: 1.2em;
    background-color: var(--the-green);
    color: var(--the-white);
    position: relative;
  }

  dialog .close {
    position: absolute;
    top: 0.5em;
    right: 0.5em;
    text-decoration: none;
    font-size: 1.4em;
    line-height: 1em;
    width: 1em;
    display: block;
    text-align: center;
    vertical-align: middle;
    color: var(--the-white);
  }

  dialog content {
    padding: 1.7em !important;
  }

  dialog footer {
    padding: 1em 3em 2em;
  }

  dialog pre {
    max-width: 850px;
    white-space: pre-line;
  }
</style>

<iframe width="0" height="0" border="0" name="dummyframe" id="dummyframe" style="display: none;"></iframe>

<section>
  <h2>Register to My EAE</h2>

  <p>
    Complete the form below to view My EAE with your saved analysis, receive occasional Energy Access Explorer
    updates, which may include information about new datasets, project updates,
    energy access newsletters, and energy access events.
  </p>
</section>

<script type="module" src="/javascripts/subscribe.js"></script>
